package: preflight_kit_api
generate:
  models: true
  embedded-spec: true
output-options:
  skip-prune: true
  exclude-schemas:
    - ExecutionModification
additional-imports:
  - package: "github.com/google/uuid"
